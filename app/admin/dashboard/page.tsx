import { UnifiedDashboard } from "@/components/unified-dashboard";
import { FC } from "react";

const page: FC = () => {
    return (
        <div>
            <UnifiedDashboard />
        </div>
    );
}

export default page;