# Jobs API

Manages job data, including job creation, assignment, and status updates.

## Endpoints

*   `GET /api/jobs`
*   `GET /api/jobs/:id`
*   `POST /api/jobs`
*   `PUT /api/jobs/:id`
*   `DELETE /api/jobs/:id`
