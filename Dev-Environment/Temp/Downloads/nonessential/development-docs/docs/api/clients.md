# Clients API

Manages client data, including profiles, service requests, and hour balances.

## Endpoints

*   `GET /api/clients`
*   `GET /api/clients/:id`
*   `POST /api/clients`
*   `PUT /api/clients/:id`
*   `DELETE /api/clients/:id`
